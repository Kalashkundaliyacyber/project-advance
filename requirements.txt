# ScanWise AI v5.0 — Python 3.10+ compatible (Kali Linux / Ubuntu / Debian)
# AI: Gemini 2.5 Flash-Lite (primary) + Ollama llama3.2 (fallback)
# CVE: Local DB + NVD API v2 enrichment

anyio==3.7.1
click==8.1.7
h11==0.14.0
httpx==0.27.0          # HTTP client (used for health checks and future integrations)
idna==3.7
jinja2==3.1.4
MarkupSafe==2.1.5
pydantic==1.10.21
python-dotenv==1.0.1   # auto-loads .env file
python-multipart==0.0.9
slowapi==0.1.9         # rate limiting
sniffio==1.3.1
starlette==0.27.0
typing_extensions==4.12.2
uvicorn==0.24.0
fastapi==0.104.1

# Report generation
reportlab==4.2.5
python-docx==1.1.2

# NVD integration — stdlib only (urllib, sqlite3, json, threading)
# No extra packages required for NVD API v2 client.
# SQLite cache uses Python's built-in sqlite3 module.
# All HTTP handled by stdlib urllib.request.

# ── AI providers ───────────────────────────────────────────────────────────────
# Gemini: uses stdlib urllib — NO extra package required.
# Get a free API key at: https://aistudio.google.com/apikey
# Free tier: 1,500 req/day · 1M tokens/minute · no credit card.
#
# Optional: install the official SDK if you prefer it over urllib:
#   pip install google-generativeai
#
# Ollama (local fallback): start with `ollama serve && ollama pull llama3.2`
# Ollama is 100% free, runs on your machine, no API key needed.

# ── NVD API key (optional) ──────────────────────────────────────────────────────
# Get a free NVD API key at: https://nvd.nist.gov/developers/request-an-api-key
# Add it to your .env file as: NVD_API_KEY=<your-key>
# Without a key: 5 req/30s rate limit (still works, just slower for large scans)
# With a key: 50 req/30s (10× throughput, recommended for full_tcp/ultimate_recon)

